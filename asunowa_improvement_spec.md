# 明日の環（アスノワ） 災害支援Webアプリ 改善仕様書

## 0. 文書情報

- プロジェクト名：明日の環（アスノワ）
- 種別：災害時の共助・物資支援マッチングWebアプリ
- 文書目的：既存プロトタイプを、認証・ユーザー管理・通報・管理者機能・レスポンシブUIを備えた実用的なプロトタイプへ改善するための実装仕様を定義する。
- 想定技術：Next.js / Supabase / Vercel
- 認証：Supabase Authを利用し、Google・LINEを利用する。
- デモ：ログインせずに閲覧できる「デモモード」を提供する。
- 重要方針：既存の機能・デザイン・データ構造は、明示的に変更を指定した部分以外は可能な限り維持する。

---

# 1. 今回の改善方針

今回の改修では、既存の「支援者デモ」「被災者デモ」を中心としたプロトタイプから、実際のユーザーがアカウントを作成して利用できる構成へ移行する。

主な変更点は以下。

1. 全体UIをレスポンシブWebデザインへ改善
2. PCでは上部タブナビゲーションを採用
3. 災害レベルをヘッダーへ移動
4. Google / LINEログインを追加
5. ログインなしでもアプリを体験できるデモモードを維持
6. ユーザーごとの表示名を投稿・チャット等に表示
7. 被災者・支援者・共助の3役割に対応
8. 投稿・地図ピン・チャットユーザーへの通報機能を追加
9. 通報理由を選択式にする
10. 一定数以上の通報で対象投稿を一時非表示
11. 管理者による災害レベル・通報・ユーザー管理を追加
12. ユーザーによる投稿履歴の編集・削除機能を追加
13. 地図投稿への画像アップロード・表示を修正
14. チャットの送り手・受け手を正しく表示
15. ヘルプセンターを完全削除

---

# 2. ユーザー区分・権限

## 2.1 ログイン状態

ユーザーは以下の3種類。

### A. 未ログイン（デモユーザー）

ログインせずにアプリを利用できる。

**現在は試作・お試し期間であるため、ログインユーザーと同じ基本機能を利用可能とする。**

したがって、未ログインでも以下を利用できる。

- ホーム
- 地図
- 検索
- チャット
- 投稿
- 通報
- その他、一般利用者向け機能

ただし、実際の個人アカウントに紐づく情報が必要な機能については、既存のデモモード仕様を利用する。

### B. ログインユーザー

GoogleまたはLINEで認証する。

ログイン後、初回のみ以下を設定する。

1. 表示名
2. 登録地域
3. 役割

### C. 管理者

一般ユーザーとは別の管理者権限を持つ。

管理者権限はユーザー自身が選択することはできない。
開発者がSupabase側で明示的にadmin権限を付与する。

---

# 3. アカウント作成・認証

## 3.1 ログイン方法

以下の2方式を提供する。

- Google
- LINE

さらに、

- ログインせずに見る（デモ）

を提供する。

---

## 3.2 初回アカウント作成フロー

### Googleの場合

```text
Googleでログイン
      ↓
Supabase Authで認証
      ↓
アスノワ内部ユーザーIDを生成
      ↓
初回ユーザー設定
      ↓
表示名を入力
      ↓
登録地域を選択
      ↓
役割を選択
  ・被災者
  ・支援者
  ・被災者＋支援者（共助）
      ↓
利用開始
```

LINEの場合も同じ初回設定フローを使用する。

---

## 3.3 表示名

「ユーザー名」は、公開用の「表示名（ニックネーム）」を意味する。

例：

```text
表示名：あすのわ太郎
```

Google/LINEから取得した氏名を、そのまま公開表示しない。

表示名はユーザー自身が設定する。

表示名は以下で利用する。

- 検索ページの投稿
- 地図投稿
- チャット
- 必要に応じたその他のユーザー表示箇所

匿名表示は不可とする。

---

# 4. Google・LINEアカウント連携

GoogleとLINEは、同一人物が両方を利用できるようにする。

## 4.1 内部ユーザーID

認証プロバイダーのIDとは別に、アスノワ独自のユーザーIDを持つ。

例：

```text
アスノワ user_id = 12345
```

---

## 4.2 LINE連携フロー

すでにGoogleでアカウントを作成しているユーザーがLINEを連携する場合：

```text
Googleでログイン
↓
アスノワ user_id = 12345
↓
「LINEを連携」
↓
LINE認証
↓
LINE認証情報をuser_id=12345へ紐付け
↓
Google / LINEのどちらからでも
user_id=12345へログイン可能
```

同一人物のGoogle・LINEログインによって別ユーザーを新規作成しない。

認証情報とアスノワのプロフィール情報を分離して管理する。

---

# 5. 役割

ユーザーの役割は3種類。

1. 被災者
2. 支援者
3. 被災者＋支援者（共助）

## 5.1 基本的な投稿種別

- 被災者 → 「依頼」
- 支援者 → 「提供」

「被災者＋支援者（共助）」は、状況に応じて依頼・提供の双方を利用できる。

---

# 6. 登録地域と災害レベル

## 6.1 登録地域

ユーザーはアカウント作成時に地域を登録する。

登録地域はマイページから変更可能とする。

GPSによる現在地ではなく、**ユーザーが登録した地域**を基準とする。

---

## 6.2 災害レベル

全国の市区町村を初期データとして登録する。

初期値：

```text
災害レベル = Lv0
```

管理者が市区町村ごとの災害レベルを変更できる。

ユーザーのヘッダーには、登録地域の災害レベルを表示する。

---

# 7. 全体UI

## 7.1 ナビゲーション

PC・スマートフォンともに主要ナビゲーションを常時表示する。

```text
[ホーム] [地図] [検索] [チャット] [マイページ]
```

PCでは、従来の横メニューボタンを廃止し、上部タブとして表示する。

スマートフォンでも横方向に押しやすい形で表示する。

---

## 7.2 ヘッダー

登録地域の災害レベルをフッターからヘッダーへ移動する。

スマートフォンでは、

```text
登録地域
   ↓
災害地域：○○市
災害レベル：Lv○
```

のように、ヘッダー内の「災害地域」の右隣に表示する。

PCでも同様に、ユーザーが現在の登録地域と災害レベルを常に認識できる位置に配置する。

---

## 7.3 レスポンシブ対応

PC、タブレット、スマートフォンでレイアウトが崩れないこと。

特にスマートフォンで以下が正常に表示されること。

- チャット
- マイページ
- 登録地域
- 災害レベル
- 上部タブ
- 投稿カード
- 地図
- 通報ボタン

文字・ボタン・カードが画面外にはみ出さないようにする。

---

# 8. ホーム

既存ホームの構成は、今回指定した変更以外は維持する。

登録地域の災害レベルはヘッダーに表示し、フッターには表示しない。

---

# 9. 地図

## 9.1 基本仕様

既存の地図・ピン機能は原則として変更しない。

ピンは誰でも作成できる。

---

## 9.2 ピンへの通報

地図上のピンを選択すると、ピン詳細を表示する。

その中に、

```text
[通報]
```

を設置する。

ワンクリックで即通報にはしない。

```text
通報
↓
通報理由を選択
↓
確認
↓
通報送信
↓
「通報しました」
```

---

## 9.3 写真

地図投稿に画像を添付できる。

### 対応形式

- PNG
- JPG
- JPEG

### サイズ

最大2.5MB。

### 保存先

Supabase Storage。

### 枚数

1投稿につき1枚まで。

### スマートフォン

スマートフォンのカメラ撮影または端末内画像を選択してアップロード可能。

### 表示

投稿後、地図上のピンを選択した際に画像を表示する。

現在発生している「ファイル選択後に地図上で画像が表示されない」問題を修正する。

---

# 10. 検索

## 10.1 投稿

検索ページの投稿は全員が利用できる。

- 未ログイン
- 被災者
- 支援者
- 共助ユーザー

投稿種別：

- 依頼
- 提供

基本ルール：

- 被災者 → 依頼
- 支援者 → 提供
- 共助 → 依頼・提供の双方

---

## 10.2 投稿者表示

投稿には必ず投稿者の表示名を表示する。

例：

```text
あすのわ太郎

お米を探しています
```

匿名表示は禁止。

---

## 10.3 投稿期限

投稿期限は設定しない。

---

## 10.4 検索条件

既存の検索機能を維持する。

例：

- 物資名
- 市町村名
- その他、既存実装済みの検索条件

今回の改修で検索条件自体は変更しない。

---

# 11. 投稿履歴

マイページに新しく「投稿履歴」を追加する。

```text
マイページ
↓
投稿履歴
↓
自分の投稿一覧
```

## 11.1 編集

自分が投稿した内容のみ編集可能。

他人の投稿は編集できない。

## 11.2 削除

自分が投稿した内容のみ削除可能。

他人の投稿は削除できない。

投稿の削除は、ユーザーによる自主削除として扱う。

---

# 12. 支援する・依頼する

投稿に対して、

- 【支援する】
- 【依頼する】

を選択できる。

この操作を行った場合のみ、関係するユーザー間の個人チャットを開設する。

---

# 13. チャット

## 13.1 チャットの定義

チャットは、投稿に対して「支援する」または「依頼する」を選択したユーザー同士の個人チャットとする。

以下は現時点では実装しない。

- 不特定多数の1対多掲示板
- 投稿コメント欄
- 地域別掲示板

---

## 13.2 送り手・受け手

現在のデモでは、支援者モード・被災者モードを切り替えても送信者・受信者の表示が同じになっている。

これを修正する。

チャット画面では、

- 現在ログインしているユーザー → 自分側
- 相手ユーザー → 相手側

として正しく表示する。

例：

```text
相手
「物資をお届けできます」

        自分
        「ありがとうございます」
```

送信者IDをメッセージデータに保存し、現在のユーザーIDと比較して左右の表示を決定する。

---

# 14. 通報

通報対象：

1. 検索ページの投稿
2. 地図のピン
3. チャットのユーザー

## 14.1 通報フロー

ワンクリック通報は禁止。

```text
[通報]
↓
通報理由選択
↓
内容確認
↓
[通報する]
↓
「通報しました」
```

1ユーザーにつき、同一対象への通報は1回まで。

---

## 14.2 通報理由

選択肢を固定する。

- 不適切な内容
- 虚偽・誤情報
- 迷惑行為
- 危険な内容
- 個人情報の掲載
- 重複投稿
- その他

「その他」を選択した場合は、必要に応じて自由記述欄を表示する。

---

## 14.3 多数通報

同一投稿が5人以上のユーザーから通報された場合：

```text
5件以上の通報
↓
対象投稿を一時非表示
↓
管理者が確認
```

管理者による確認前は「削除」ではなく「一時非表示」とする。

---

# 15. 管理者機能

管理者専用ページ：

```text
/admin
```

管理者ページを以下の3カテゴリに分ける。

1. 災害・レベル管理
2. 通報・投稿管理
3. ユーザー管理

---

# 16. 管理者認証・権限チェック

## 16.1 アプリ側

```text
ログインしている？
      ↓
     YES
      ↓
role === "admin"？
   ↙       ↘
 YES        NO
 ↓           ↓
管理者画面   利用者画面へ
```

未ログイン・一般ユーザーが管理者ページへアクセスした場合、管理者画面を表示しない。

---

## 16.2 データベース側

UIを隠すだけでは不十分。

SupabaseのRLS（Row Level Security）等を利用し、

```text
一般ユーザー
↓
管理用データの変更不可

admin
↓
管理用データの変更可能
```

となるようにする。

管理者権限は開発者がSupabase側で付与する。

例：

```text
profiles
id: xxxx
role: admin
```

一般ユーザーの登録時は自動的に一般利用者として作成する。

ユーザーが自分自身でadmin権限を設定できないようにする。

---

# 17. 管理者：災害・レベル管理

既存の災害レベル管理UIを基本的に維持する。

ただし、全国の市区町村を初期登録する。

初期状態：

```text
全国の市区町村
↓
すべてLv0
```

管理者が地域ごとにレベルを変更できる。

---

# 18. 管理者：通報・投稿管理

通報された対象を一覧表示する。

最低限、以下を表示する。

- 通報対象
- 対象投稿者
- 通報者
- 通報理由
- 通報内容
- 通報日時
- 対象の種類（投稿 / 地図ピン / チャットユーザー）
- 現在の状態

---

## 18.1 管理者の対応

管理者は以下を選択できる。

### 削除

通報内容を確認し、問題があると判断した場合。

対象投稿をDBから完全削除する。

### 却下

通報内容を確認し、問題がないと判断した場合。

投稿は公開状態に戻す。

---

## 18.2 一時非表示

5件以上通報された投稿は、管理者確認まで一時非表示。

管理者が確認した後、

```text
削除
```

または

```text
却下 → 再公開
```

とする。

---

# 19. 管理者：ユーザー管理

登録ユーザーを一覧表示する。

表示項目：

- 名前（表示名）
- メール
- 権限
- 地域
- アカウント状態
- アクション

権限：

- 被災者
- 支援者
- 被災者＋支援者（共助）
- admin（管理者）

アカウント状態：

- 活動中
- 凍結中

---

# 20. ユーザー凍結

管理者はユーザーを、

- 凍結
- 凍結解除

できる。

## 20.1 凍結中に利用できる機能

- ログイン → ○
- 地図閲覧 → ○
- 検索閲覧 → ○

以下は禁止。

- 投稿
- チャット
- 通報

---

## 20.2 凍結ユーザーへの表示

ログイン時に以下の内容を表示する。

> ご利用のアカウントは、凍結されています。慎重に審査した結果、明日の環ルールに違反していると判断いたしました。そのため、ログイン・地図閲覧・検索閲覧は可能ですが、投稿・チャット・通報機能はご利用いただけません。ご理解の程、よろしくお願いいたします。

凍結中は、利用できない操作をUI上でも明確にする。

---

# 21. ヘルプセンター

「ヘルプセンター」ページを完全に削除する。

以下すべてから削除する。

- ページ
- URL / route
- ナビゲーション
- ボタン
- 関連リンク
- フッター等に存在するリンク
- 不要になった関連コンポーネント

削除後、404やリンク切れが発生しないことを確認する。

---

# 22. 推奨データベース構造

既存データベースを可能な限り活用し、新規テーブルが必要な場合のみ追加する。

## 22.1 profiles

```text
profiles
├ id
├ display_name
├ role
├ region
├ account_status
├ created_at
└ updated_at
```

role：

```text
user / admin
```

ユーザーの利用役割は別項目として、

```text
victim
supporter
both
```

のいずれかを保持する。

account_status：

```text
active
frozen
```

---

## 22.2 認証情報

Supabase Authを使用する。

Google・LINEの認証情報と、公開プロフィール情報を分離する。

Google/LINEのprovider情報から表示名を直接公開しない。

---

## 22.3 投稿

既存投稿テーブルを利用する。

必要に応じて以下を保持する。

```text
post_id
user_id
post_type
content
location
image_url
status
created_at
updated_at
```

post_type：

```text
request
offer
```

status：

```text
published
hidden
deleted
```

---

# 23. 通報データ

通報履歴は、管理者が確認できるよう保存する。

概念上、以下を保持する。

```text
report_id
reporter_user_id
target_type
target_id
reason
detail
status
created_at
```

target_type：

```text
post
map_pin
chat_user
```

status：

```text
pending
deleted
rejected
```

同一ユーザーが同一対象を複数回通報できないよう、DB側でも一意制約を検討する。

---

# 24. チャットデータ

概念上、

```text
conversation
├ conversation_id
├ post_id
├ requester_id
├ provider_id
└ created_at
```

message：

```text
message_id
conversation_id
sender_id
message
created_at
```

チャット表示時は、

```text
message.sender_id === current_user.id
```

なら自分側、それ以外なら相手側として表示する。

---

# 25. 重要なセキュリティ要件

生成AIによる実装時に、以下を必ず守る。

1. SupabaseのService Role Key等の秘密情報をクライアントへ露出させない。
2. 管理者権限をフロントエンドだけで判定しない。
3. Supabase RLS等でDBアクセスを制御する。
4. ユーザーが自分でadmin権限を設定できないようにする。
5. 他人の投稿を編集・削除できないようにする。
6. 他人のプロフィール情報を勝手に変更できないようにする。
7. 通報の重複送信をDB側でも防止する。
8. 凍結ユーザーがAPI経由で投稿・チャット・通報できないようにする。
9. アップロード画像について、拡張子だけでなくMIME type・サイズも検証する。
10. 画像ファイル名や保存パスをユーザー入力のまま信頼しない。
11. 管理者操作について、可能なら操作履歴を残せる設計にする。
12. 削除処理は、対象ユーザーが他人のデータを削除できないようにする。

---

# 26. 画面一覧

## 利用者

```text
/
├── ホーム
├── 地図
├── 検索
├── チャット
├── マイページ
│   ├── プロフィール
│   ├── 登録地域
│   ├── 役割
│   └── 投稿履歴
│       ├── 投稿確認
│       ├── 編集
│       └── 削除
└── ログイン
    ├── Google
    ├── LINE
    └── ログインせずに見る
```

## 管理者

```text
/admin
├── dashboard
├── disaster-level
├── reports
├── posts
└── users
```

ヘルプセンター関連routeは削除する。

---

# 27. 実装上の重要ルール

## 27.1 既存機能を壊さない

今回明示的に変更していない既存機能については、原則として維持する。

既存コードを大幅に作り直す前に、現在の構造を確認する。

---

## 27.2 モバイルファーストで確認

PCだけで動作確認して完了としない。

最低限、

- PC
- タブレット相当
- スマートフォン

で確認する。

特に以下を重点確認する。

- ヘッダー
- タブ
- 災害地域
- 災害レベル
- チャット
- マイページ
- 地図
- 投稿カード
- 通報ダイアログ

---

# 28. 実装優先順位

生成AIで一度にすべてを変更して既存機能を壊さないよう、以下の順で実装する。

## Phase 1：UI基盤

- レスポンシブ対応
- PC上部タブ
- スマホ上部タブ
- ヘッダーへ災害レベル移動
- ヘルプセンター削除

## Phase 2：認証・ユーザー

- Supabase Auth
- Google
- LINE
- アスノワuser_id
- profiles
- 表示名
- 地域
- 役割
- Google/LINE連携

## Phase 3：投稿

- 投稿者表示
- 投稿履歴
- 編集
- 削除
- 投稿権限

## Phase 4：チャット

- マッチング後の個人チャット
- sender_id
- 送り手・受け手表示修正

## Phase 5：地図

- 画像アップロード修正
- Supabase Storage
- 画像表示
- 通報

## Phase 6：通報

- 通報理由
- 重複防止
- 5件以上で一時非表示
- 管理者確認
- 削除・却下

## Phase 7：管理者

- admin認証
- RLS
- 災害レベル管理
- 通報・投稿管理
- ユーザー管理
- 凍結・解除

---

# 29. 受け入れ条件（完成判定）

以下をすべて満たした場合を今回の改修完了とする。

### 認証

- Googleで新規登録できる
- LINEで新規登録できる
- GoogleとLINEを同一アカウントへ連携できる
- ログインせずデモ利用できる

### ユーザー

- 表示名を設定できる
- 地域を設定できる
- 被災者・支援者・共助を設定できる
- 投稿に表示名が表示される

### UI

- PCで上部タブが常時表示される
- スマートフォンでもレイアウトが崩れない
- 災害レベルがヘッダーに表示される

### 投稿

- 全員が投稿できる
- 被災者は依頼を投稿できる
- 支援者は提供を投稿できる
- 共助は双方を利用できる
- 自分の投稿を編集できる
- 自分の投稿を削除できる

### チャット

- 支援する・依頼するから個人チャットを開始できる
- 送り手と受け手が正しく左右に表示される

### 地図

- ピンを作成できる
- PNG/JPG/JPEGを2.5MB以内でアップロードできる
- Supabase Storageに保存される
- ピン選択時に画像が表示される

### 通報

- 投稿を通報できる
- 地図ピンを通報できる
- チャットユーザーを通報できる
- 通報理由を選択できる
- 同一ユーザーによる同一対象への重複通報を防止できる
- 5件以上で対象投稿が一時非表示になる

### 管理者

- adminのみ管理画面を利用できる
- DB側でも権限チェックされる
- 全国市区町村がLv0で登録されている
- 災害レベルを変更できる
- 通報を確認できる
- 削除・却下を選択できる
- ユーザー一覧を確認できる
- ユーザーを凍結・解除できる

### 凍結

- 凍結ユーザーはログインできる
- 地図・検索を閲覧できる
- 投稿・チャット・通報ができない
- 凍結理由を画面に表示する

### ヘルプセンター

- ページが存在しない
- ナビゲーションから消えている
- 関連リンクが残っていない
- リンク切れがない

---

# 30. 生成AIへの実装指示

この仕様書を使用して既存アプリを改修する場合、以下を厳守する。

1. 既存コードを確認してから変更する。
2. 明示されていない既存機能を勝手に削除しない。
3. 既存のデータを破壊しない。
4. DBスキーマ変更が必要な場合は、既存テーブルとの互換性を確認する。
5. 認証・権限・RLSをUI実装より優先して安全に設計する。
6. フロントエンドでの権限非表示だけを「セキュリティ対策」としない。
7. Google/LINEの認証情報と公開表示名を分離する。
8. GoogleとLINEが同一アスノワユーザーに紐付く設計を維持する。
9. 凍結・通報・削除の状態をDBとUIの両方で一貫させる。
10. モバイル表示を必ず確認する。
11. エラー発生時に既存機能を壊さない。
12. 実装後、受け入れ条件を一つずつ確認する。
13. 不明点がある場合、既存実装を確認してから判断し、既存仕様と矛盾する独自仕様を勝手に追加しない。
14. 特に「デモユーザー」「ログインユーザー」「admin」の権限を混同しない。
15. 実装前に、必要なDB変更・環境変数・OAuth設定・Storage設定を一覧化する。
16. 秘密鍵・OAuth Secret・Supabase Service Role Key等をGitHubへコミットしない。

---

# 31. 今回の仕様の基本コンセプト

本アプリは、災害時に「支援したい人」と「支援を必要としている人」をつなぐ共助プラットフォームである。

そのため、UIだけを改善するのではなく、

**「誰が」「どの立場で」「何を投稿し」「誰とつながり」「誰が管理するのか」**

を一貫したデータ構造・権限管理で実現する。

最終的には、

```text
                 明日の環
                    │
          ┌─────────┴─────────┐
          ↓                   ↓
       利用者                管理者
          │                   │
    ┌─────┼─────┐       ┌─────┼─────┐
    ↓     ↓     ↓       ↓     ↓     ↓
   地図   検索  チャット  災害Lv 通報  ユーザー
    │     │     │
    └─────┼─────┘
          ↓
    「支援する」「依頼する」
          ↓
       個人チャット
          ↓
        共助の実現
```

という一貫した体験を実現する。
