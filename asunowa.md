# 明日の環（アスノワ） 災害時共助アプリ 仕様書
## 改善・修正版 / アプリ生成AI向け実装仕様

> **この仕様書の目的**
>
> 既存の災害時共助Webアプリ「よりそい」を「明日の環（アスノワ）」へ改善・修正するための仕様書です。
> 既存機能を勝手に削除・変更せず、以下に明記した変更点のみを確実に反映してください。
> 実装時に不明点がある場合は、既存仕様との整合性を優先し、独自判断で大きく仕様変更しないでください。
>
> **重要：既存機能の維持**
>
> 以下の既存の主要機能は維持してください。
> - 支援者・被災者間の個人間物資マッチング
> - 災害時道路状況共有マップ
> - 災害レベルによる地域ごとの機能制限
>
> 特に「リアルタイム災害道路状況共有マップ」は**既存機能・変更なし**です。

---

# 1. アプリ概要

## 1.1 アプリ名

旧名称：

`よりそい`

新名称：

# `明日の環（アスノワ）`

UI上では「明日の環」を基本表示とし、必要に応じて読み方として「アスノワ」を使用してください。

## 1.2 ロゴ・アイコン

旧「よりそい」の左隣に表示されているアイコンを、`asunowa.png` に変更してください。

- 既存のロゴ表示位置・サイズ・余白・レイアウトは原則維持
- `asunowa.png` を正しく読み込む
- 画像が存在しない場合に勝手な代替画像を生成しない
- 既存UIとの視認性を確認する

---

# 2. アプリの主要機能

以下の3機能を中核機能として維持する。

## 2.1 物資マッチング機能

支援者と被災者が、個人間で物資の「提供」「依頼」を行い、マッチングできる機能。

イメージはメルカリのような個人間取引UI。

ただし、本アプリでは金銭の売買ではなく、災害時の物資支援を目的とする。

### 役割

- 被災者：物資を「依頼」
- 支援者：物資を「提供」

この表現をアプリ全体で統一する。

## 2.2 災害時道路状況共有マップ

災害状況・道路状況をマップ上でリアルタイムに確認できる既存機能。

**今回の改修では変更しない。**

## 2.3 災害レベル機能

地域ごとに災害レベルを設定し、レベルに応じて一部機能を制限する。

---

# 3. ユーザー役割

ユーザーには以下の2種類がある。

- 被災者
- 支援者

## 3.1 役割の切り替え

ユーザーはマイページから、

**「被災者」⇔「支援者」**

を切り替えられるようにする。

### 操作場所

`マイページ > 災害時の自己申告・役割設定`

ここから現在の役割を確認・変更できる。

### 重要

役割変更後は、検索ページ・投稿ページ・チャットなど、役割によって表示・利用条件が変わるすべてのUIに即時反映する。

---

# 4. 災害発生地域

## 4.1 登録

ユーザーはアカウント登録時に「災害発生地域」を入力・選択できる。

例：

```text
災害発生地域：鳥取県米子市
```

また、登録後も、

`マイページ > 災害時の自己申告・役割設定`

から再登録・変更できる。

## 4.2 地域レベルの自動取得

ユーザーが「鳥取県米子市」を登録した場合、

**管理者が設定した「鳥取県米子市」の災害レベルを自動取得する。**

ユーザーがレベルを直接入力する仕様にはしない。

---

# 5. 災害レベル管理

## 5.1 管理者による地域別設定

管理者ページで、チェックリスト形式で地域を選択し、それぞれの地域に災害レベルを設定できるようにする。

イメージ：

```text
□ 東京都
   □ 港区
   □ 渋谷区
   □ 新宿区
   ...

□ 鳥取県
   □ 米子市
   □ 鳥取市
   □ 境港市
   ...
```

## 5.2 市区町村単位

災害レベルは**市区町村単位**で設定する。

例：

```text
東京都
├ 港区 Lv3
├ 渋谷区 Lv2
└ 新宿区 Lv0

鳥取県
├ 米子市 Lv1
├ 鳥取市 Lv2
└ 境港市 Lv0
```

## 5.3 デフォルト

**何も設定されていない地域は必ずLv0とする。**

管理者が設定を追加した地域だけ、設定されたレベルになる。

## 5.4 レベルの取得

ユーザーの「災害発生地域」と、管理者が設定した市区町村のレベルを照合し、現在の地域レベルを自動取得する。

---

# 6. フッターの災害レベル表示

フッターに、ユーザーが登録した「災害発生地域」と、その地域の現在の災害レベルを表示する。

例：

```text
鳥取県米子市　災害レベル1
```

ユーザーが地域を変更した場合は、その変更を反映する。

未設定地域の場合はLv0として表示する。

---

# 7. 検索ページ

## 7.1 被災者UIの変更

被災者UIで「支援を投稿する」と表示されている箇所は、

**「依頼を投稿する」**

に変更する。

## 7.2 投稿の種類を明確化

すべての投稿に、小さく以下のラベルを表示する。

- `提供`
- `依頼`

### 投稿者とラベル

```text
支援者 → 【提供】
被災者 → 【依頼】
```

## 7.3 表示する投稿

ユーザーの役割によって検索結果を絞り込む。

### 被災者

**支援者の「提供」投稿のみ表示**

```text
被災者
 ↓
検索
 ↓
支援者の【提供】のみ
```

### 支援者

**被災者の「依頼」投稿のみ表示**

```text
支援者
 ↓
検索
 ↓
被災者の【依頼】のみ
```

自分自身の投稿を検索結果に表示する必要はない。

## 7.4 投稿背景色

投稿の種類を視覚的にも区別する。

- 被災者の「依頼」：薄いオレンジ
- 支援者の「提供」：薄い緑

色だけに意味を依存せず、必ず「提供」「依頼」のテキストラベルも表示する。

---

# 8. 検索結果の並び順

全国の投稿を単純に最新順で表示する仕様から変更する。

ユーザーが登録した「災害発生地域」を基準として、投稿を近い順に表示する。

## 8.1 優先順位

優先順位は必ず以下とする。

```text
① 地域の近さ
② 緊急度
③ 投稿日時
```

つまり、

1. 地域が近い投稿を優先
2. 地域の近さが同程度なら、緊急度が高い投稿を優先
3. それも同じなら、新しい投稿を優先

とする。

## 8.2 距離の計算方法

地域の近さは、

**市区町村の中心座標同士の距離**

で判定する。

例：

```text
ユーザーの災害発生地域：米子市
投稿の受け取り場所：鳥取市

米子市中心座標
       ↓
     距離計算
       ↓
鳥取市中心座標
```

道路経路距離ではなく、市区町村の中心座標同士の距離を基準とする。

---

# 9. 投稿機能

## 9.1 投稿の種類

投稿は以下の2種類。

### 被災者

`依頼`

必要な物資を投稿する。

### 支援者

`提供`

提供できる物資を投稿する。

この用語をアプリ全体で統一する。

---

# 10. カテゴリ

投稿カテゴリを以下に統一する。

```text
食料
飲料
衣類
医薬品
生活用品
電気機器
その他
```

新規追加：

- 電気機器
- その他

既存カテゴリは可能な限り維持する。

---

# 11. 投稿の利用制限

## 11.1 判定対象

投稿時に入力された、

**「受け取り場所」**

の市区町村の災害レベルを判定する。

投稿者自身の現在地では判定しない。

例：

```text
支援者：大阪府
受け取り場所：熊本県○○市

↓

熊本県○○市の災害レベルを取得

↓

そのレベルに応じて投稿可否を判定
```

## 11.2 レベル2以下

受け取り場所のレベルが、

- Lv0
- Lv1
- Lv2

の場合、投稿可能。

被災者・支援者の双方が、役割に応じて投稿できる。

```text
被災者 → 依頼を投稿
支援者 → 提供を投稿
```

## 11.3 レベル3

受け取り場所がLv3の場合、支援依頼・提供の投稿機能を利用できない。

投稿画面上で以下のメッセージを表示する。

```text
この機能はレベル3以下の時のみ使用できます。
安全な状態になるまでお待ち下さい。
```

※ 実際のレベルを動的に表示する場合は、

```text
この機能はレベル2以下の時のみ使用できます。
安全な状態になるまでお待ち下さい。
```

のように、利用可能な上限を明示する。

---

# 12. レベル3へ変更された場合の既存投稿

すでに存在する投稿についても、対象地域がLv3になった場合は、安全上の理由から支援開始操作をできない状態にする。

## 12.1 「支援する」ボタン

Lv3の投稿では、

`支援する`

ボタンを押せない状態にする。

ユーザーが操作しようとした場合は、以下を表示する。

```text
この機能はレベル2以下の時のみ利用できます。
安全な状態になるまでお待ち下さい。
```

※ ボタンを完全に非表示にするのではなく、「現在利用できない」ことが分かるUIを優先する。

---

# 13. チャット機能

## 13.1 メインメニュー

メインメニューに、

**「チャット」**

を新しく追加する。

UIイメージはメルカリの取引チャットを参考にする。

## 13.2 チャット開始条件

チャットは以下の順番で開始される。

```text
投稿
 ↓
応募
 ↓
マッチ成立
 ↓
チャット開始
```

投稿を見ただけではチャットは開始しない。

## 13.3 マッチ成立

1つの依頼に対して、成立する支援者は**1人**とする。

つまり、

```text
被災者A
「水を10本希望」

      ↓

支援者B → 応募
支援者C → 応募
支援者D → 応募

      ↓

マッチ成立

      ↓

支援者Bと被災者Aのチャット開始
```

成立後は、同一依頼に対して別の支援者との新規マッチを成立させない。

---

# 14. 支援開始・取引状態

チャットには、支援の進行状態を表示する。

## 14.1 状態1：支援前

マッチ成立後、支援者がまだ支援を開始していない状態。

表示：

```text
[支援を開始する（配達・移動開始）]
```

このボタンは**支援者UIのみ**表示する。

## 14.2 状態2：支援中

支援者が「支援を開始する」を押した状態。

表示：

```text
【支援中】
[支援完了確認]
```

「支援を開始する」ボタンは再度押せない。

つまり、支援開始は一度だけ実行可能。

## 14.3 状態3：支援完了

被災者・支援者の双方が「支援完了確認」を押した状態。

表示：

```text
【取引完了】
```

この状態になったら個人間チャットを終了する。

---

# 15. 支援完了確認

## 15.1 ボタン

支援開始後、

**被災者・支援者の両方**

に、

`支援完了確認`

ボタンを表示する。

## 15.2 両者が押した場合

```text
支援者 → 支援完了確認 ✓
被災者 → 支援完了確認 ✓

↓

取引完了

↓

チャット終了
```

## 15.3 片方だけ押した場合

片方だけが確認した場合、すぐにはチャットを終了しない。

例：

```text
支援者 → 完了確認 ✓
被災者 → 未確認
```

この場合、チャットは継続する。

**片方だけが押した状態から7日間経過した場合、自動的に取引・チャットを終了する。**

---

# 16. チャットのレベル制限

チャット中の投稿の「受け取り場所」の災害レベルがLv3になった場合、安全上の理由から、

**「支援を開始する（配達・移動開始）」ボタンを利用できない状態**

にする。

ユーザーが操作しようとした場合：

```text
この機能はレベル2以下の時のみ利用できます。
安全な状態になるまでお待ち下さい。
```

を表示する。

## 16.1 重要

すでに支援中の場合の状態を勝手にリセットしない。

つまり、

```text
Lv2
 ↓
支援開始
 ↓
【支援中】
 ↓
Lv3に変更
```

となった場合、

**「支援中」という状態そのものは維持する。**

ただし、再度「支援開始」を実行できないようにするなど、状態に矛盾が生じないようにする。

---

# 17. 画面・UI構成

## 17.1 メインメニュー

既存のメインメニューを維持しつつ、

**チャット**

を追加する。

主要な導線：

```text
ホーム
検索
投稿
チャット
マイページ
```

既存アプリに上記以外のメニューがある場合、それを勝手に削除しない。

## 17.2 マイページ

以下を確認・変更できるようにする。

### 災害時の自己申告・役割設定

- 被災者 / 支援者
- 災害発生地域

災害発生地域を変更すると、その地域の災害レベルを自動取得する。

---

# 18. 管理者ページ

管理者ページに地域別災害レベル設定機能を追加する。

## 18.1 地域選択

都道府県 → 市区町村の階層で表示する。

例：

```text
▼ 鳥取県
  □ 米子市
  □ 鳥取市
  □ 境港市

▼ 東京都
  □ 港区
  □ 渋谷区
  □ 新宿区
```

## 18.2 レベル設定

各市区町村について災害レベルを設定できる。

```text
米子市
[ Lv0 ▼ ]

鳥取市
[ Lv2 ▼ ]

境港市
[ Lv3 ▼ ]
```

## 18.3 未設定地域

管理者が設定していない市区町村はLv0。

---

# 19. データ・状態管理

生成AIによる実装時には、画面だけでなくデータ状態を正しく管理すること。

最低限、以下の概念を保持する。

## User

```text
user
- id
- role: victim | supporter
- disaster_prefecture
- disaster_city
```

## Post

```text
post
- id
- user_id
- type: request | offer
- category
- title
- description
- received_location
- received_prefecture
- received_city
- urgency
- created_at
- status
```

## DisasterLevel

```text
disaster_level
- prefecture
- city
- level
- updated_at
```

## Match

```text
match
- id
- post_id
- victim_id
- supporter_id
- status
- created_at
```

## Chat

```text
chat
- id
- match_id
- victim_id
- supporter_id
- status
- support_started_at
- victim_completed_at
- supporter_completed_at
- completed_at
```

---

# 20. 状態遷移

## 20.1 支援の状態

```text
マッチ成立
    ↓
【支援前】
    ↓
支援者が「支援を開始する」
    ↓
【支援中】
    ↓
被災者・支援者がそれぞれ「支援完了確認」
    ↓
両者確認
    ↓
【取引完了】
    ↓
チャット終了
```

## 20.2 7日間のタイムアウト

```text
支援完了確認
      ↓
片方のみ確認
      ↓
7日経過
      ↓
自動終了
```

両者確認の場合は7日を待たずに即時終了。

---

# 21. 実装上の重要ルール

## 21.1 UIだけで制限しない

災害レベルによる利用制限は、ボタンを無効化するだけではなく、実際の投稿・支援開始処理でもサーバー側でチェックする。

例えばLv3の地域に対して、

```text
POST /create-post
POST /start-support
```

などの処理を直接実行されても、許可しない。

## 21.2 現在のレベルを常に再確認

投稿時・支援開始時には、その時点での対象地域の災害レベルを確認する。

過去にLv2だったからといって、現在Lv3でも操作できる状態にしない。

## 21.3 ユーザーの地域と受け取り場所を混同しない

以下は別のデータとして扱う。

```text
ユーザーの災害発生地域
→ 検索結果の距離計算・フッター表示などに使用

投稿の受け取り場所
→ 投稿・支援開始の利用可否判定に使用
```

---

# 22. 既存機能を壊さないためのルール

今回の改修では、以下を特に注意する。

- 既存の認証機能を勝手に変更しない
- 既存の道路状況マップ機能を変更しない
- 既存の投稿データを勝手に削除しない
- 既存のAPI・データベース構造を、必要性なく全面変更しない
- 既存のレスポンシブデザインを維持する
- PC・スマートフォンの両方で表示を確認する
- 既存の機能が動作する状態を維持したまま新機能を追加する
- 既存データとの互換性を考慮する
- 既存の認証ユーザーがアップデート後も利用できるようにする

---

# 23. UIデザイン方針

本アプリは災害時に使用するため、以下を重視する。

### 優先順位

1. 分かりやすさ
2. 安全性
3. 操作のしやすさ
4. 情報の視認性
5. デザイン性

過度なアニメーションや装飾を追加せず、緊急時でも迷わないUIにする。

## 投稿の識別

必ず、

```text
【提供】
【依頼】
```

のテキストラベルを表示する。

背景色は補助的な識別方法として使用する。

- 提供：薄い緑
- 依頼：薄いオレンジ

色覚特性に配慮し、色だけで情報を伝えない。

---

# 24. 実装前チェックリスト

生成AIは実装前に以下を確認すること。

### ユーザー

- [ ] 被災者 / 支援者をマイページから切り替えられる
- [ ] 災害発生地域を登録できる
- [ ] 災害発生地域をマイページから変更できる
- [ ] 地域変更後、災害レベルが自動更新される

### 災害レベル

- [ ] 市区町村単位で設定できる
- [ ] 未設定地域はLv0
- [ ] 管理者ページから設定できる
- [ ] フッターに地域＋レベルを表示する

### 検索

- [ ] 被災者には提供のみ表示
- [ ] 支援者には依頼のみ表示
- [ ] 各投稿に提供 / 依頼ラベルがある
- [ ] 背景色でも区別できる
- [ ] 地域の近さを最優先
- [ ] 同程度なら緊急度
- [ ] それも同じなら投稿日時

### 投稿

- [ ] 被災者は依頼
- [ ] 支援者は提供
- [ ] 電気機器カテゴリがある
- [ ] その他カテゴリがある
- [ ] 受け取り場所の地域レベルを判定
- [ ] Lv2以下で投稿可能
- [ ] Lv3では投稿不可

### マッチング

- [ ] 投稿 → 応募 → マッチ成立 → チャット開始
- [ ] 1つの依頼につき成立する支援者は1人

### チャット

- [ ] メインメニューにチャットがある
- [ ] 支援者のみ支援開始ボタンを表示
- [ ] 支援開始は1回のみ
- [ ] 支援中状態を表示
- [ ] 両者に支援完了確認ボタンを表示
- [ ] 両者確認で取引完了
- [ ] 両者確認後チャット終了
- [ ] 片方のみ確認した場合は7日間維持
- [ ] 7日後に自動終了
- [ ] Lv3では支援開始できない

### 既存機能

- [ ] 道路状況共有マップを変更していない
- [ ] 既存認証が動作する
- [ ] 既存投稿データが維持される
- [ ] PCで動作する
- [ ] スマートフォンで動作する

---

# 25. 生成AIへの最終指示

この仕様書を基準として既存アプリを改修してください。

**重要：新しいアプリをゼロから作り直すのではなく、現在のアプリの構造・デザイン・既存機能を確認したうえで、必要な箇所を修正・追加してください。**

実装前に、

1. 現在のファイル構成
2. 現在の認証方式
3. データベース構造
4. 投稿データ構造
5. ユーザー情報
6. 災害レベルの現在の実装
7. マップ機能
8. 既存のルーティング
9. API・環境変数
10. 既存のUI構造

を確認してください。

そのうえで、既存機能を破壊しない最小限の変更で実装してください。

**特に、画面だけを変更して終わりにせず、ユーザー役割・地域・災害レベル・投稿・マッチング・チャット・支援状態が一貫して連動するように実装してください。**

また、実装後は以下のケースを必ずテストしてください。

### テストケース1：通常状態

```text
ユーザー：米子市
米子市：Lv1
投稿受け取り場所：米子市

→ 投稿可能
→ 支援開始可能
```

### テストケース2：危険地域

```text
ユーザー：米子市
米子市：Lv3
投稿受け取り場所：米子市

→ 投稿不可
→ 支援開始不可
→ 指定メッセージ表示
```

### テストケース3：別地域

```text
ユーザー：大阪市
大阪市：Lv0

投稿受け取り場所：熊本県○○市
熊本県○○市：Lv3

→ ユーザー自身がLv0でも、
  受け取り場所がLv3なので支援開始不可
```

### テストケース4：チャット完了

```text
マッチ成立
↓
支援開始
↓
支援中
↓
支援者：完了確認
↓
被災者：完了確認
↓
取引完了
↓
チャット終了
```

### テストケース5：片方だけ確認

```text
支援中
↓
支援者：完了確認
↓
被災者：未確認
↓
7日間経過
↓
自動終了
```

---

# 完成イメージ

最終的に、

```text
                  明日の環（アスノワ）
                         │
          ┌──────────────┼──────────────┐
          ↓              ↓              ↓
       被災者           支援者         管理者
          │              │              │
          │              │        地域別Lv設定
          │              │              │
          └──────┬───────┘              │
                 ↓                      │
             物資マッチング ←───────────┘
                 │
          ┌──────┴──────┐
          ↓             ↓
        提供            依頼
          │             │
          └──────┬──────┘
                 ↓
              応募
                 ↓
             マッチ成立
                 ↓
               チャット
                 ↓
        支援を開始する
                 ↓
               支援中
                 ↓
       両者が完了確認
                 ↓
              取引完了
                 ↓
             チャット終了
```

という一貫したユーザー体験になるように実装してください。
